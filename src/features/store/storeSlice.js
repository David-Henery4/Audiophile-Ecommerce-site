import { createSlice } from "@reduxjs/toolkit";
// import initialStoreData from "../../../starter-code/data.json"

const initialState = {

}

const storeSlice = createSlice({
  name: "store",
  initialState
})

export default storeSlice.reducer